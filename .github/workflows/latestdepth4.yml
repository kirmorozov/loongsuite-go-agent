# This workflow will build a golang project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-go

name: Latest

on:
  push:
    branches: [ "main", "dev" ]
    paths:
      - 'test/**'
      - 'pkg/**'
      - 'tool/**'
      - '.github/workflows/**'
  pull_request:
    branches: [ "main" ]
    paths:
      - 'test/**'
      - 'pkg/**'
      - 'tool/**'
      - '.github/workflows/**'

jobs:

  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        # Go 1.24 is kept deliberately: modules declare `go 1.25.0` (required by
        # OTel >= 1.42), so a 1.24 runner exercises the GOTOOLCHAIN=auto upgrade
        # path that users on 1.24 rely on. Go 1.26 joins the matrix now that
        # bytedance/sonic is on v1.15.x - v1.14.1 gated GoMapIterator behind
        # !go1.26, which broke the eino and hertz builds.
        # See https://github.com/bytedance/sonic/issues/895
        go_version:
          - 1.24
          - 1.25
          - 1.26
    steps:
    - uses: actions/checkout@v4

    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: ${{ matrix.go_version }}

    - name: Build
      run: make build

    - name: Clean Go Caches
      run: |
        go clean -cache
        go clean -modcache

    - name: Test
      run: go test -timeout 50m -v github.com/alibaba/loongsuite-go/test -run "TestLatest4"
